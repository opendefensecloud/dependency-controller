{{- if and .Values.tls.certManager.enabled (not .Values.tls.existingSecret) }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "dependency-webhook.tlsSecretName" . }}
  labels:
    {{- include "dependency-webhook.labels" . | nindent 4 }}
spec:
  secretName: {{ include "dependency-webhook.tlsSecretName" . }}
  dnsNames:
    - {{ include "dependency-webhook.serviceFQDN" . }}
    - {{ include "dependency-webhook.serviceFQDN" . }}.cluster.local
    {{- range .Values.tls.certManager.additionalDnsNames }}
    - {{ . }}
    {{- end }}
  issuerRef:
    name: {{ required "tls.certManager.issuerRef.name is required when cert-manager is enabled" .Values.tls.certManager.issuerRef.name }}
    kind: {{ .Values.tls.certManager.issuerRef.kind | default "ClusterIssuer" }}
{{- end }}
