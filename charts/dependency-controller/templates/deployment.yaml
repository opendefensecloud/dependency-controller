apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "dependency-controller.fullname" . }}
  labels:
    {{- include "dependency-controller.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      {{- include "dependency-controller.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "dependency-controller.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "dependency-controller.serviceAccountName" . }}
      containers:
        - name: controller
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - /dependency-controller
          args:
            - --api-export-name={{ .Values.apiExportName }}
            {{- if .Values.kcpBaseHost }}
            - --kcp-base-host={{ .Values.kcpBaseHost }}
            {{- end }}
            - --webhook-service-account-name={{ .Values.webhook.serviceAccount.name }}
            - --webhook-service-account-namespace={{ .Values.webhook.serviceAccount.namespace | default .Release.Namespace }}
            {{- if .Values.webhook.url }}
            - --webhook-url={{ .Values.webhook.url }}
            {{- end }}
            {{- if .Values.webhook.caBundle.secretName }}
            - --webhook-ca-bundle-path=/etc/webhook-ca/ca.crt
            {{- end }}
            - --health-probe-bind-address={{ .Values.healthProbeBindAddress }}
          {{- if .Values.kubeconfig.secretName }}
          env:
            - name: KUBECONFIG
              value: /etc/kcp-kubeconfig/{{ .Values.kubeconfig.key }}
          {{- end }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
          volumeMounts:
            {{- if .Values.kubeconfig.secretName }}
            - name: kcp-kubeconfig
              mountPath: /etc/kcp-kubeconfig
              readOnly: true
            {{- end }}
            {{- if .Values.webhook.caBundle.secretName }}
            - name: webhook-ca
              mountPath: /etc/webhook-ca
              readOnly: true
            {{- end }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      volumes:
        {{- if .Values.kubeconfig.secretName }}
        - name: kcp-kubeconfig
          secret:
            secretName: {{ .Values.kubeconfig.secretName }}
        {{- end }}
        {{- if .Values.webhook.caBundle.secretName }}
        - name: webhook-ca
          secret:
            secretName: {{ .Values.webhook.caBundle.secretName }}
            items:
              - key: ca.crt
                path: ca.crt
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
