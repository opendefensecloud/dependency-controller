apiVersion: apis.kcp.io/v1alpha1
kind: APIResourceSchema
metadata:
  name: v260410-7199b0b.dependencies.dependencies.opendefense.cloud
spec:
  group: dependencies.opendefense.cloud
  names:
    kind: Dependency
    listKind: DependencyList
    plural: dependencies
    singular: dependency
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      description: |-
        Dependency is a marker object that records a specific dependency relationship
        between two resource instances. Created automatically by the dependency controller.
      properties:
        apiVersion:
          description: |-
            APIVersion defines the versioned schema of this representation of an object.
            Servers should convert recognized schemas to the latest internal value, and
            may reject unrecognized values.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
          type: string
        kind:
          description: |-
            Kind is a string value representing the REST resource this object represents.
            Servers may infer this from the endpoint the client submits requests to.
            Cannot be updated.
            In CamelCase.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
          type: string
        metadata:
          type: object
        spec:
          description: DependencySpec identifies the specific dependent and dependency
            resource instances.
          properties:
            dependency:
              description: Dependency identifies the resource being depended upon
                (protected from deletion).
              properties:
                group:
                  description: Group is the API group of the resource.
                  type: string
                name:
                  description: Name is the name of the resource instance.
                  type: string
                resource:
                  description: Resource is the plural resource name.
                  type: string
                version:
                  description: Version is the API version of the resource.
                  type: string
              required:
              - group
              - name
              - resource
              - version
              type: object
            dependent:
              description: Dependent identifies the resource that holds the reference.
              properties:
                group:
                  description: Group is the API group of the resource.
                  type: string
                name:
                  description: Name is the name of the resource instance.
                  type: string
                resource:
                  description: Resource is the plural resource name.
                  type: string
                version:
                  description: Version is the API version of the resource.
                  type: string
              required:
              - group
              - name
              - resource
              - version
              type: object
            ruleRef:
              description: RuleRef identifies the DependencyRule that caused this
                Dependency to be created.
              properties:
                cluster:
                  description: Cluster is the logical cluster name of the workspace
                    where the DependencyRule lives.
                  type: string
                name:
                  description: Name is the name of the DependencyRule.
                  type: string
              required:
              - cluster
              - name
              type: object
          required:
          - dependency
          - dependent
          - ruleRef
          type: object
      required:
      - spec
      type: object
    served: true
    storage: true
    subresources: {}
